@*@using DataContext.DAL;
@using DataContext.Until;
@{
    var model = (List<Video>)Model;
}*@



<div class="carousel-inner">
    @*@if (model.Count > 0)
    {
        if (model.Count >= 5)
        {
            for (int i = 0; i < 5; i++)
            {
                <div class="item @Html.Raw(i==0?"active":"")">
                    <a href="@Html.Raw("lib-image/" + ConvertTextToLink.ConvertUrlsToLinks(model[i].tenchude) + "-" + model[i].id)"><img src="@Html.Raw(model[i].link)"></a>
                </div>

            }
        }
        else
        {
            for (int i = 0; i < model.Count; i++)
            {
                <div class="item @Html.Raw(i==0?"active":"")">
                    <a href="@Html.Raw("lib-image/" + ConvertTextToLink.ConvertUrlsToLinks(model[i].tenchude) + "-" + model[i].id)"><img src="@Html.Raw(model[i].link)"></a>
                </div>
            }
        }
    }
    else
    {*@
        <div class="item active">
            <img src="~/images/bd-hoi-nghi.jpg" class="img-responsive" alt="nha trang">
        </div>
  @*  }*@
</div>

<!-- Left and right controls -->
<a class="left carousel-control" href="#myCarousel" data-slide="prev">
    <span class="glyphicon glyphicon-chevron-left"></span>
    <span class="sr-only">Previous</span>
</a>
<a class="right carousel-control" href="#myCarousel" data-slide="next">
    <span class="glyphicon glyphicon-chevron-right"></span>
    <span class="sr-only">Next</span>
</a>
