@using DataContext.DAL;
@using DataContext.Until;
@{
    var model = (List<DataContext.m_diadiemtochuc>)Model;
}






<div class="carousel-inner">
    @if (model.Count > 0)
    {
        if (model.Count >= 5)
        {
            for (int i = 0; i < 5; i++)
            {
                <div class="item @Html.Raw(i==0?"active":"")">
                    @*<a href="@Html.Raw("detail-new/" + ConvertTextToLink.ConvertUrlsToLinks(model[i].tieude) + "-" + model[i].id)"><img src="@Html.Raw(model[i].avatar)"></a>*@
                  @*  <img src="@Html.Raw(model[i].avatar)">*@
                    <a href="/assa-vn"><img src="@Html.Raw(model[i].avatar)"></a>
                </div>

            }
        }
        else
        {
            for (int i = 0; i < model.Count; i++)
            {
                <div class="item @Html.Raw(i==0?"active":"")">
                    @*<a href="@Html.Raw("detail-new/" + ConvertTextToLink.ConvertUrlsToLinks(model[i].tieude) + "-" + model[i].id)"><img src="@Html.Raw(model[i].avatar)"></a>*@
                  @*  <img src="@Html.Raw(model[i].avatar)">*@
                    <a href="/assa-vn"><img src="@Html.Raw(model[i].avatar)"></a>
                </div>
            }
        }
    }
    else
    {
        <div class="item active">
            <img src="~/images/banner.jpg" alt="Los Angeles">
        </div>

        <div class="item">
            <img src="~/images/banner.jpg" alt="Los Angeles">
        </div>

        <div class="item">
            <img  src="~/images/banner.jpg" alt="Los Angeles">
        </div>
        <div class="item">
            <img src="~/images/banner.jpg" alt="Los Angeles">
        </div>

        <div class="item">
            <img src="~/images/banner.jpg" alt="Los Angeles">
        </div>
    }
</div>

<a class="left carousel-control" href="#myCarousel" data-slide="prev">
    <span class="glyphicon glyphicon-chevron-left"></span>
    <span class="sr-only">Previous</span>
</a>
<a class="right carousel-control" href="#myCarousel" data-slide="next">
    <span class="glyphicon glyphicon-chevron-right"></span>
    <span class="sr-only">Next</span>
</a>
